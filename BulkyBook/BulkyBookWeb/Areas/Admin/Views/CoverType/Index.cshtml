@model IEnumerable<CoverType>
@{
	ViewData["Title"] = "Cover Type";
}

<div class="container p-3">
	<div class="row p-4">
		<div class="col-6">
			<h2 class="text-primary">Cover Type Lista</h2>
		</div>
		<div class="col-6 text-end">
			<a asp-controller="CoverType" asp-action="Create" class="btn btn-primary">
				Criar novo Cover Type
			</a>
		</div>
	</div>

	<table class="table table-bordered table-striped" style="width=100%">
		<thead>
			<tr>
				<th>
					Cover Type
				</th>
				@*<th>
					Display Order
				</th>
				<th>
					Created Data
				</th>*@
				<th></th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<td width="30%">
						@item.Name
					</td>
					@*<td width="30%">
						@item.DisplayOrder
					</td>
					<td width="30%">
						@item.CreatedDateTime.ToString("dd/MM/yyyy HH:mm")
					</td>*@
					<td>
						<div class="w-75 btn-group" role="group">
							<a asp-controller="CoverType" asp-action="Edit" asp-route-id="@item.Id" class="btn btn-primary mx-2">Edit</a>
							<a asp-controller="CoverType" asp-action="Delete" asp-route-id="@item.Id" class="btn btn-danger mx-2">Delete</a>
						</div>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>

